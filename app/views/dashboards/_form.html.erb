<%= form_with(model: dashboard, local: true) do |form| %>
  <% if dashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard.errors.count, "error") %> prohibited this dashboard from being saved:</h2>

      <ul>
      <% dashboard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :dashboard_title, size: 40 %>
  </div>

  <div class="field">
    <%= form.label :public %>
    <%= form.check_box :public, id: :dashboard_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
